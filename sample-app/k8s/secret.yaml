# DB password (Domain 4: Secret). PLACEHOLDER ONLY — replace at deploy time, never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: sample-secret
  namespace: sample
type: Opaque
stringData:
  DB_PASSWORD: "changeme-placeholder"
